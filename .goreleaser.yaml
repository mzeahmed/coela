# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

# Always named "coela", regardless of the repository's own name.
project_name: coela

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: coela
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    # Injects version metadata into main's package-level vars, consumed
    # later by a `coela version` command.
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - formats: [tar.gz]
    # Matches the OS/Arch naming `uname` would report.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

# Version string used for local builds (`--snapshot`), which have no git tag.
snapshot:
  version_template: "{{ .Version }}-SNAPSHOT-{{ .ShortCommit }}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^ci:"
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  draft: false
  prerelease: auto