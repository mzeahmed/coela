# Database credentials consumed by docker-compose.yml (${DB_*}).
# This file is gitignored: keep real secrets out of version control.
DB_DATABASE={{ .Name }}
DB_USER={{ .Name }}
DB_PASSWORD={{ .Name }}
{{- if ne .Database "postgres" }}
DB_ROOT_PASSWORD=root
{{- end }}